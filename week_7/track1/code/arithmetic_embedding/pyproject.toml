[project]
name = "arithmetic-embedding"
version = "0.2.0"
description = "Research prototypes for exact and learned latent arithmetic"
requires-python = ">=3.10"
dependencies = ["torch>=2.2"]

[tool.pytest.ini_options]
testpaths = ["tests"]
